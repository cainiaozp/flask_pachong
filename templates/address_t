这是一个网页的HTML代码片段。该页面是一个招聘数据分析系统。
代码以通常的doctype声明开始，然后是一个语言设置为English的打开HTML标记。然后是一个带有各种元标签的标题部分，
一个带有文本“学历招聘排行”，一个指向外部样式表的链接标记，以及一个链接到Font Awesome库的脚本标记。
在head部分之后，body标签打开，类设置为“sb nav fixed”。然后，代码定义了一个深色背景的导航栏，
其中包含左侧的徽标、菜单切换按钮和用于用户信息的下拉菜单。菜单有两个链接：“首页“（主页）和”搜索“（搜索）。
接下来，有一个ID为“layoutSidenav”的div，其中包含一个侧边栏导航菜单。此菜单是一个深色背景的可折叠手风琴，
包含各种页面的链接，包括“首页“（主页）和”搜索“（搜索），以及标题为”招聘数据可视化“（招聘数据可视化），并带有标题为“time_t”的页面链接。

